'use strict';

const fs = require('fs-es6-promise');
const staticHandler = exports;

staticHandler.extension = "static";

staticHandler.handle = async function(component, scriptPath) {

	return fs.readFile("." + scriptPath);

};
